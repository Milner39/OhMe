import config from "./WebServer/svelte.config.mts"
export default config

/*
    This file only exists because as of now, there is no way to tell
    the SvelteKit-Vite plugin where the config file is. This is a workaround.

    Github issue: https://github.com/sveltejs/kit/issues/2973
*/